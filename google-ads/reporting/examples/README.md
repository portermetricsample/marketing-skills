# Examples — placeholder

Ready-made dashboards / reports that a user can **duplicate with the Porter Metrics MCP**.
Each example ships the built output so it can be replicated end to end.

🔴 To be populated. **Fictional data only** (e.g. Acme Insurance) — never real client accounts.
