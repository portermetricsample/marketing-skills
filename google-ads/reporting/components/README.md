# Components — placeholder

Reusable **graphic components** (charts, tables, scorecards) that the `dashboard/` and `slides/`
builders draw on. Because the source (`porter-reporting`) is private, the components a public user
needs must live **here**, declared in the public repo.

🔴 To be populated (e.g. keyword-ad-landing alignment, impression-share trend, search-term n-grams).
Styling comes from the public `porter-design` repo — components carry structure/logic, not brand CSS.
